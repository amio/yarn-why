{
  "name": "yarn-why",
  "version": "0.3.2",
  "description": "Identifies why a package has been installed.",
  "repository": "amio/yarn-why",
  "bin": "bin/yarn-why",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "pretest": "standard *.js bin/*",
    "snap": "TAP_SNAPSHOT=1 npm run test",
    "test": "tap test/*.spec.js --reporter spec --test-env=FORCE_COLOR=0"
  },
  "dependencies": {
    "@yarnpkg/lockfile": "^1.1.0",
    "kleur": "^3.0.2",
    "mri": "^1.1.4"
  },
  "devDependencies": {
    "execa": "2",
    "standard": "14",
    "tap": "14"
  }
}
